extern crate rsgraph;
use rsgraph::graph::{DiGraph, contains_cycle};

fn main() {
    // let di_graph = DiGraph::from(&[1,2,3,4], &[(1,2), (2,3), (3,1)]);
    // assert_eq!(contains_cycle(&di_graph), true);
    // let nodes : Vec<usize> = (1..30_000).collect();
    // let edges = [(1,2), (2,1500), (1500,1501), (1501, 1502), (1502, 1)];
    // let di_graph : DiGraph<usize> = DiGraph::from(&nodes, &edges);
    // println!("Contains cycles: {:?}", contains_cycle(&di_graph));
}
